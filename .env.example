# Personal Assistant — environment template
# Copy to `.env` and fill in as needed. `.env` is gitignored.
#
# NOTE for Phase 1:
#   - The Telegram bot token is stored by the Telegram channel plugin
#     (via `/telegram:configure <token>`), NOT here.
#   - The official Todoist MCP uses OAuth (browser sign-in), NOT a token here.
#   So for Phase 1 this file is intentionally empty of real secrets.
#
# Reserved for later phases / the community token-based Todoist server:
# TODOIST_API_TOKEN=

# Hy-Vee grocery cart automation (scripts/hyvee/login_test.py).
# Fill these in your local .env (never commit real values).
HYVEE_USERNAME=
HYVEE_PASSWORD=

# Home Assistant MCP server (vendor/ha-mcp) — long-lived access token from
# Home Assistant -> Profile -> Security -> Long-lived access tokens.
# Run scripts/register_ha_mcp_task.ps1 once these are filled in.
HOMEASSISTANT_URL=
HOMEASSISTANT_TOKEN=
