{
  "_comment": "Copy to .mcp.json (gitignored). Official Todoist MCP: hosted HTTP + OAuth. No token stored here; OAuth sign-in happens in-browser on first tool use. Do NOT also run `claude mcp add todoist ...` if this file exists — duplicate entries cause OAuth sign-in loops.",
  "mcpServers": {
    "todoist": {
      "type": "http",
      "url": "https://ai.todoist.net/mcp"
    }
  }
}
